# [PRD] 간병인 및 요양보호사 중계 플랫폼 초소형 MVP

## 1. 프로젝트 개요 (Overview)
- **제품명 (가칭)**: 케어매치 (CareMatch) Pre-MVP
- **목적**: 보호자(환자 가족)와 간병인/요양보호사 간의 빠른 매칭 가설을 최단 기간 내 최소 비용으로 검증.
- **핵심 가설**: "보호자가 돌봄 조건을 등록하고, 검증된 간병인이 지원하여 매칭이 성사되는가?"

---

## 2. 타겟 유저 및 페르소나 (Target Users)

1. **보호자 (환자 가족)**
   - **Needs**: 급하게 병원/자택 간병인이 필요함. 간병인의 경력, 자격증, 성별, 비용을 명확히 확인하고 싶음.
   - **Pain Point**: 기존 간병 협회의 깜깜이 매칭, 높은 수수료, 검증되지 않은 간병인 프로필.

2. **간병인 / 요양보호사**
   - **Needs**: 본인의 거주지/일정에 맞는 간병 일자리를 빠르게 구하고 싶음.
   - **Pain Point**: 불투명한 알선 과정, 공고 확인의 어려움.

---

## 3. 핵심 사용자 여정 (User Journey)

### 3.1 보호자 여정
1. 메인 화면 진입 → **[간병 신청하기]** 클릭
2. 간병 요청서 작성 (환자 상태, 기간, 장소, 희망 일당 등)
3. 신청 완료 후 지원한 간병인 목록/프로필 확인
4. 마음에 드는 간병인에게 **[매칭 확정/상담 요청]**

### 3.2 간병인 여정
1. 메인 화면 진입 → **[간병 공고 보기]**
2. 지역/조건에 맞는 공고 탐색
3. 공고 상세 확인 후 **[지원하기]** 클릭 (본인 프로필 제출)
4. 보호자 매칭 완료 시 알림 수신

---

## 4. 상세 기능 요구사항 (Functional Requirements)

| 모듈 | 기능명 | 상세 내용 | 우선순위 |
| :--- | :--- | :--- | :--- |
| **공통** | 회원가입/로그인 | 휴대폰 번호 인증 또는 소셜 로그인 (카카오) | P0 |
| **보호자** | 간병 요청서 작성 | - 환자 정보: 성별, 연령대, 장소(병원/자택), 돌봄 등급/상태<br>- 필요 서비스: 석션, 식사 보조, 거동 보조 등<br>- 기간 및 희망 일당 설정 | P0 |
| **보호자** | 지원자 목록 조회 | - 해당 공고에 지원한 간병인 프로필 카드 목록<br>- 자격증, 경력, 자기소개 확인 | P0 |
| **보호자** | 매칭 선택 | 특정 간병인 선택하여 매칭 확정 요청 | P0 |
| **간병인** | 프로필 등록 | - 이름, 연락처, 활동 가능 지역, 보유 자격증, 경력 사항, 한줄 소개 | P0 |
| **간병인** | 공고 목록 조회 | - 등록된 간병 요청서 리스트 (지역, 일당, 환자 상태 요약) | P0 |
| **간병인** | 공고 지원하기 | - 원클릭 지원하기 | P0 |
| **알림/운영** | 매칭 알림 | - 지원 발생 시 보호자에게 SMS/알림톡 발송<br>- 매칭 확정 시 간병인에게 안내 발송 | P1 |

---

## 5. 데이터 모델 (Data Model - 초안)

### 5.1 Users (사용자)
- `user_id` (PK)
- `role` (보호자 / 간병인)
- `name`, `phone_number`, `created_at`

### 5.2 Caregiver_Profiles (간병인 프로필)
- `profile_id` (PK)
- `user_id` (FK)
- `certifications` (자격증 목록: 요양보호사 1급 등)
- `experience_years` (경력 연수)
- `location` (활동 지역)
- `bio` (자기소개)

### 5.3 Care_Requests (간병 요청서)
- `request_id` (PK)
- `user_id` (FK - 보호자)
- `patient_gender`, `patient_age_group`
- `location_type` (병원 / 자택)
- `location_address`
- `care_level` (환자 상태 / 필요한 보조 수준)
- `start_date`, `end_date`
- `daily_pay` (희망 일당)
- `status` (모집중 / 매칭완료 / 취소)

### 5.4 Applications (지원/매칭 내역)
- `application_id` (PK)
- `request_id` (FK)
- `caregiver_id` (FK - 간병인 user_id)
- `status` (지원완료 / 매칭확정 / 거절)
- `created_at`

---

## 6. 성공 지표 (KPI & Validation Metrics)

1. **신청 전환율**: 메인 방문 대비 간병 요청서 작성 완료 비율 (목표: > 15%)
2. **지원 비율**: 등록된 공고 당 평균 간병인 지원 수 (목표: 공고당 2명 이상)
3. **매칭 성공률**: 생성된 요청서 중 실제 매칭 성사 비율 (목표: > 50%)
